import Foundation

enum AuthEndpoints {
    static let register = "/api/auth/register/"
    static let me = "/api/auth/me/"
    static let token = "/api/token/"
    static let refresh = "/api/token/refresh/"
}

